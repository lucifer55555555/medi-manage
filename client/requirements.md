## Packages
framer-motion | Page transitions and layout animations
recharts | Dashboard analytics and charts
lucide-react | Icons for the interface (already in stack, but noting for usage)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Cal Sans", "Inter", "sans-serif"],
}
Authentication uses Replit Auth (provided via hook).
API endpoints are defined in shared/routes.ts.
